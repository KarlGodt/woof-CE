#One or more words that identify this distribution:
DISTRO_NAME='Lighthouse64'
#A three-digit numeric value, version number of this distribution:
DISTRO_VERSION=514
#A two-digit numeric value, minor-version number of this distribution:
DISTRO_MINOR_VERSION=2
#The distro whose binary packages were used to build this distribution:
DISTRO_BINARY_COMPAT='slackware64'
#Prefix for some filenames: exs: quirkysave.2fs, quirky-004.sfs
DISTRO_FILE_PREFIX='L64'
#The version of the distro whose binary packages were used to build this distro:
DISTRO_COMPAT_VERSION='13.37'

#Puppy default filenames...
#Note, the 'SFS' files below are what the 'init' script in initrd.gz searches for,
#for the partition, path and actual files loaded, see PUPSFS and ZDRV in /etc/rc.d/PUPSTATE
DISTRO_PUPPYSFS="${DISTRO_FILE_PREFIX}-${DISTRO_VERSION}.sfs"
DISTRO_ZDRVSFS=''
DISTRO_IDSTRING='L111203113643ZZZZ514XXXXXXXXXXXX'
