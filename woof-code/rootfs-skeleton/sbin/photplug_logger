#!/bin/ash

eval `env`
[ "$ACTION" = remove ] && SUFFIX=rem || SUFFIX=add
echo "
$0 : $$ $PPID
`ps-FULL -o pid,ppid,s,args | grep -w -E \"${$}|${PPID}\"`
---------- $@ --------------
`date +%T:%N`

`env`

`dmesg|tail`
">> "/tmp/hotplug.${SUFFIX}.log"
