
if [[ $reason == "CARRIER" ]] ; then
pidof yaf-splash && killall yaf-splash
ssid="`wpa_cli -i $interface status|grep ^ssid|cut -d= -f2`"

yaf-splash -display ":0" -margin 2 -bg violet -placement top -font "9x15B" -outline 0 -timeout 3 -text "Requesting IP Address for $interface $ssid"
fi
