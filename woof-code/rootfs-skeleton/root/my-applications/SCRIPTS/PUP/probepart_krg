#!/bin/bash

#***
#
#***

PPs=`cat /proc/partitions | sed '1,2d' |awk '{print $4}'`
grepP=`echo $PPs |sed 's@ @|\^@g'`
SBs=`ls -1 /sys/block | grep -vE "^loop|^md|^ram|$grepP"`
DRall="$PPs
$SBs"
#DRall=`echo "$DRall" |sort -u`

echo "$DRall"
