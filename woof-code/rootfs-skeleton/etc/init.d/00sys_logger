#!/bin/ash


case $1 in
 start)
 (
  pidof syslogd || { syslogd -m 0;syslogd -O /var/log/syslog.7 -l 7 -s 1024 -b 99; }
  pidof klogd   || klogd
 ) >/dev/null
 ;;
 stop)
 (
  pidof klogd   && killall klogd
  pidof syslogd && killall syslogd
 ) >/dev/null
 ;;
esac
