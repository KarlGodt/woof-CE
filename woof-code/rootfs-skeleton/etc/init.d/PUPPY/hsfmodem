#!/bin/ash
#Barry Kauler 2009. totally free code.

[ "$*" ] || exit
[ "$1" = "start" ] || exit
#if a modem found, quit...
[ -h /dev/modem ] && exit

[ "`lsmod | grep '^hsfosspec'`" ] || exit

ln $VERB -sf ttySHSF0 /dev/modem

###END###
