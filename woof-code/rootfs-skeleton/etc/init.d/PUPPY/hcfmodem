#!/bin/sh
#Barry Kauler 2009. totally free code.

#if a modem found, quit...
[ -h /dev/modem ] && exit
[ ! $1 ] && exit
[ "$1" != "start" ] && exit
[ "`lsmod | grep '^hcfpcihw'`" = "" ] && exit

ln -snf ttySHCF0 /dev/modem

###END###
