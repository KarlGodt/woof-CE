#!/bin/ash
#usb modem
#Barry Kauler, LGPL 2007

[ "$*" ] || exit
[ "$1" = "start" ] || exit
[ -h /dev/modem ] && exit

[ "`lsmod | grep '^cdc_acm'`" = "" ] && exit
ln $VERB -sf ttyACM0 /dev/modem

###END###
