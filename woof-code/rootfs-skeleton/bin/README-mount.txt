mount-FULL, umount-FULL, losetup-FULL are the full versions.
The Busybox versions of mount and umount are available but only by:

# busybox mount ...
# busybox umount ...

mount, umount are now scripts.
