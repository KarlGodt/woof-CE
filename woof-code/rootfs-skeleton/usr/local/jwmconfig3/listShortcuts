#!/bin/ash


SCRIPT_DIR="/usr/local/jwmconfig3"
test -f "$SCRIPT_DIR/variables" && . "$SCRIPT_DIR/variables"
. /etc/rc.d/f4puppy5

##----lists shortcuts contained in the jwmrc-personal file---->>

awk '/key/ {print $2 "_" $3}' "$personalFILE" | awk -F\> '/key/ {print $1 " " $2}' | awk -F\< '/key/ {print $1}' >"$keyL"

# Very End of this file 'usr/local/jwmconfig3/listShortcuts' #
###END###
