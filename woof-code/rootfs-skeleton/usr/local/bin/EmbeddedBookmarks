#!/bin/bash


cd /usr/local/EmbeddedBookmarks

if [ ! -f $HOME/.EmbeddedBookmarks/channels2.txt ];then

        mkdir -p $HOME/.EmbeddedBookmarks
        cp -ax channels2.txt $HOME/.EmbeddedBookmarks/

fi

./EmbeddedBookmarks "$@"
