#!/bin/sh
BGOPT=""
if [ -f /root/.jwm/jwm_colors ];then #100504
. /root/.jwm/jwm_colors
 BGOPT="-bg ${MENU_BG}"
fi
killall icewmtray 2>/dev/null 
sleep 2
killall blinky 2>/dev/null
sleep 2
#blinky ${BGOPT} &
killall freememapplet_tray 2>/dev/null
sleep 1
killall firewallstate & 2>/dev/null
sleep 1
/root/Startup/firewallstate & 2>/dev/null
sleep2
killall  powerapplet_tray 2>/dev/null
/root/Startup/powerapplet_tray & 2>/dev/null

#find apps in /root/startup and execute
#if [ "$POWER" = "" ];then
#	/root/Startup/powerapplet_tray & 2>/dev/null
#fi
#FWALL=` ps|grep 'firewallstate'|grep -v 'grep'`
#if [ "$FWALL" = "" ];then
#	firewallstate & 2>/dev/null
#fi
#retrovol kick along

sleep 4
killall retrovol && retrovol -hide ${BGOPT} &
#end