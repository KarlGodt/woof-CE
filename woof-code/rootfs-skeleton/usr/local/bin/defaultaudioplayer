#!/bin/ash
DEFAULT_AUDIOPLAYER=aqualung
DEFAULT_AUDIOPLAYER_OPTIONS=--output\ alsa\ -R\ -N\ 0\ -L

case $DEFAULT_AUDIOPLAYER in
*aqualung*) $DEFAULT_AUDIOPLAYER -N 0 -T;;
esac

#exec aqualung "$@"
exec $DEFAULT_AUDIOPLAYER $DEFAULT_AUDIOPLAYER_OPTIONS "$@"
