#!/bin/sh

if test -f "$@" ; then

FILE_TYPE=`file "$@"`

else
 : #TODO
fi


case $FILE_TYPE in
*MPEG*4*|*Mpeg*4*|*mpeg*4*)
 : #exec mplayer "$@"
;;

*Flash*|*FLASH*|*flash*)
 which mplayer && exec mplayer "$@"
;;

*) :
;;
esac

exec gxineshell "$@"
