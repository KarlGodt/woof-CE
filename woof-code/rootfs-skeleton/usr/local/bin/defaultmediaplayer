#!/bin/ash
DEFAULT=gxineshell
OPTIONS=

if test -f "$@"; then
file "$@" | grep -iE 'flash video|MPEG v4' && {
exec mplayer -loop 0 "$@"
} || {
exec $DEFAULT $OPTIONS "$@"
}

else
exec $DEFAULT $OPTIONS "$@"
fi
