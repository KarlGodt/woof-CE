#!/bin/bash

echo "$1" > /tmp/mplayerplaylist

if [ -f /usr/local/bin/mplayer-git ];then

	mplayer-git -quiet -playlist /tmp/mplayerplaylist
	exit 0
fi


mplayer -quiet -playlist /tmp/mplayerplaylist

